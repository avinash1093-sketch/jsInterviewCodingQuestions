// function toUpperCaseArray(arr) 
// {
// return arr.map((elem,index) => elem.toUpperCase())
// }

// console.log(toUpperCaseArray(['g', 'f', 'g']));

function toUpperCaseArray(arr) 
{
return arr.map((elem,index) => elem.toUpperCase())
}

console.log(toUpperCaseArray(['g', 'f', 'g']));